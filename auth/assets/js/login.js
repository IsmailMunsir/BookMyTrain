document.addEventListener("DOMContentLoaded", () => {
  const form = document.getElementById("loginForm");
  form.addEventListener("submit", (e) => {
    // You can add real-time form validation here if needed
    console.log("Logging in...");
  });
});
